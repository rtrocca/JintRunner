// Test script for CLI mode
write("This is CLI mode - script will load and then start JavaScript REPL");
write("You can evaluate JavaScript expressions interactively");

// Define some variables and functions for testing
var testVar = "Hello from CLI mode";
var testArray = [1, 2, 3, 4, 5];

function testFunction(x) {
    return x * 2;
}

write("Available for testing: testVar, testArray, testFunction(x)");